{
 "recorded_utc": "2026-09-26T13:00:00Z",
 "mechanism": {
  "tracker": "scripts/orchestration/lane_status.py (read-only; state derived from workflow journals, runner outputs, validation/ files, and the fired-trigger ledger; idempotent)",
  "daemon": {
   "pid": 29739,
   "launched": "setsid nohup (PPID 1, own session)",
   "started_utc": "2026-09-26T12:57:26Z",
   "script": "<scratchpad>/orchestration/orchestration_daemon_v2.sh",
   "period_s": 60,
   "event_log": "<scratchpad>/orchestration/events.log (append-only: DAEMON_START/EXIT, STATE transitions into terminal states, TRIGGER_READY on each transition into the ready set, ALERT STALE_CLAIM / LAUNCH_UNCONFIRMED)",
   "heartbeat": "<scratchpad>/orchestration/heartbeat",
   "previous": "v1 daemon PID 27068 (12:50:51Z-12:57:29Z), replaced (not edited in place) to announce READY on transitions and to raise alerts"
  },
  "wake": {
   "tool": "harness Monitor tailing events.log (filter: TRIGGER_READY, ALERT, DAEMON_START/EXIT, TRACKER_ERROR, done_open_issues, structural_fail, verified_provisional, error)",
   "task": "bb6x36rw2",
   "expiry": "30 min (harness cap); re-armed at each expiry, when the daemon heartbeat is also checked"
  },
  "actions": "taken by the operator (this session) on a TRIGGER_READY event; each firing appended to docs/orchestration/fired_triggers.jsonl and committed",
  "ledger": "docs/orchestration/trigger_ledger_v2.jsonl + claims/ (transactional lifecycle; see OPERATING_MODEL.md section 1)"
 },
 "campaign_runners": [
  {
   "pid": 6292,
   "script": "<scratchpad>/followon/run_all.sh",
   "launched": "setsid nohup (PPID 1)",
   "started_utc": "2026-09-26T11:57:22Z",
   "queue": "5 O4 first stages then facility_mandatory",
   "log": "<scratchpad>/followon/progress.log"
  },
  {
   "pid": 21503,
   "script": "<scratchpad>/followon/run_escalation_johnsonlow.sh",
   "launched": "setsid nohup (PPID 1)",
   "started_utc": "2026-09-26T12:39:37Z",
   "queue": "3 Johnson-low escalations",
   "log": "<scratchpad>/followon/progress_escalation.log"
  }
 ],
 "workflows": {
  "wf_020ac47f-09d": "earlier lanes B-L (lanes 10, 13, 14, 30-37)",
  "wf_02471fb6-48c": "preliminary v1 forensics (completed; superseded by lanes 02, 03, 29)",
  "wf_17c9b0e6-1cb": "lanes 01, 02, 03, 29",
  "wf_2bcadd98-5ff": "lanes 07, 08, 09",
  "wf_828d7b1a-ca8": "lanes 06, 11, 12, 15",
  "wf_3da51c0c-3b4": "lanes 16, 17 -> 23",
  "wf_15f0f2f8-8d4": "lanes 28, 18, 20",
  "wf_e3c01fb8-eed": "lanes 24, 26 -> 27",
  "wf_22d6b4cf-10a": "lanes 25, 19, 21, 22"
 },
 "survival": {
  "daemon + runners": "survive tool-task deaths and session idle (detached); do NOT survive container reclamation",
  "monitor": "harness task; expires every 30 min and is re-armed; lost if the session ends",
  "workflows": "run inside this session; resumable only in the same session (resumeFromRunId); lost on container reclamation"
 },
 "restart_semantics": "On any wake or resume: check the daemon heartbeat (< 3 min) and PID; if dead, start a new daemon (state is re-derived). Triggers with a live claim (CLAIMED/LAUNCHED/VERIFIED) are never READY, so a restart can never relaunch them; STALE_CLAIM / LAUNCH_UNCONFIRMED alerts are resolved by the operator with evidence (LAUNCHED recovered, or FAILED + new attempt). Runner death shows as missing keys in the pinned structural check; missing shards are reported to the owner, never silently rerun.",
 "incidents": [
  "2026-09-26: two earlier session-bound watchers (harness background shells byu2qv4f0 started 12:40Z, bbdjklhst started 11:57Z) died silently (empty output, no exit notice; cause not determined). The detached daemon + Monitor design replaces them; no event was lost (state is derived, not event-sourced).",
  "2026-09-26T12:56:55Z: during the migration to the transactional ledger, the v1 daemon ran the tracker after it had switched to the (still empty) v2 ledger and before the reconstructed claims were written; it announced TRIGGER_READY T_O4_ESCALATE:ds_staged_n2_n_exc_johnsonlow. No action was taken; the reconstructed LAUNCHED claim removed it from the ready set and the claim CLI refuses a second claim. This is the failure mode the transactional rule guards against.",
  "2026-09-26 ~13:00Z: every workflow agent failed with 'session limit reached' (reset 13:20Z). No lane reached a terminal state; completed builds are preserved (journals + worktrees). Julia runners and the daemon were unaffected (no LLM use). 13:21Z: workflows resumed with resumeFromRunId under the SAME run ids (resume scripts embed the original args verbatim, so cached agents replay); staggered to avoid a repeat: Bundle-1 workflows (wf_2bcadd98-5ff, wf_15f0f2f8-8d4, wf_3da51c0c-3b4, wf_e3c01fb8-eed) and the physics critical path (wf_17c9b0e6-1cb) first; wf_828d7b1a-ca8, wf_22d6b4cf-10a and wf_020ac47f-09d (the latter patched for a null-verdict crash) follow when these finish."
 ],
 "execution_baseline": {
  "pinned": "branch claude/nifty-ramanujan-w68f9z at debce1603de04b79881fef4ae355e6cdbf531436",
  "main": "daa0e759e26416847f200a4781194f266be27c5c (does NOT contain the orchestration governance)",
  "rule": "until the branch is merged (owner approval), every developer and every automated step uses this branch as the execution baseline; later commits on it add only campaign results, ledger events and verified lane merges, each recorded; any change to the orchestration code or registries is announced as a new pinned baseline",
  "recorded_utc": "2026-09-26T13:08:00Z"
 },
 "known_limitations": [
  "Orphan claim file (owner review of debce16): if the process dies after the exclusive claim file is created but before the CLAIMED event is appended, O_EXCL still prevents a duplicate execution, but the tracker may show the trigger READY while every new claim of that attempt fails on the orphan file. This is a liveness/diagnostics issue, not an idempotency failure. Owner decision: address it only if it actually occurs (recovery: inspect claims/<key>.<attempt>.claim and record the event with evidence)."
 ]
}